import type { Metadata } from "next";
import { ModePage, modeMetadata } from "@/components/ModePage";
export const metadata: Metadata = modeMetadata("rust-crash-gambling-sites");
export default function Page() { return <ModePage slug="rust-crash-gambling-sites" />; }
